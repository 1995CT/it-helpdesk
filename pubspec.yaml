name: it_call_log_app
description: "IT Helpdesk - Ticket Management System"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.12.4
  google_fonts: ^6.3.0
  intl: ^0.20.2
  image_picker: ^1.1.2
  file_picker: ^8.1.0
  fl_chart: 0.68.0
  csv: ^6.0.0
  share_plus: ^12.0.2
  shared_preferences: ^2.5.3
  url_launcher: ^6.2.5
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

dependency_overrides:
  app_links: 3.5.1
  share_plus: 10.1.4
  flutter_plugin_android_lifecycle: 2.0.20

flutter:
  uses-material-design: true
